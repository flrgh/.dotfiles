#!/bin/sh

set -euo pipefail

if ! docker image inspect lsp/lua:latest &>/dev/null; then
    "$HOME/.local/libexec/install/lsp/install-lua-language-server" &>/dev/null
fi

exec docker run --init --rm -i lsp/lua:latest
